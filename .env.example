MODEL_NAME=deepfake
MODEL_VERSION=1
DEFAULT_FPS=2
MAX_FRAMES=256
THRESHOLD=0.5
REQUEST_TIMEOUT=30

# Auth (replace in prod)
REQUIRE_AUTH=true
JWT_SECRET=change-me

# Internal service URL when using Docker Compose
TF_SERVING_URL=http://tfserving:8501
